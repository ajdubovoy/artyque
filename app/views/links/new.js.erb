function refreshDashboard(innerHTML) {
  const form = document.getElementById('edit-form');
  form.innerHTML = innerHTML;
}

refreshDashboard('<%= j render "new", artist: @artist, link: @link %>');
window.init();
