.line-wrapper
	.container
		.row
			div.col.d-flex.line-left
				h4.title Featured Artwork
	  .ellipse
.container
	.row
		- @featured_artwork.each do |artwork|
			div.col-12.col-sm-6.featured-artwork-photo style="background-image: url(#{artwork.photos.first.attachment.url unless !artwork.photos.present? });" 
				.overlay
					h2.color.c-white = artwork.name
					p = artwork.description
