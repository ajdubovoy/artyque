function refreshDashboard(innerHTML) {
  const form = document.getElementById('edit-dashboard');
  form.innerHTML = innerHTML;
}

refreshDashboard('<%= j render "dashboard", artist: @artist, stage: @stage %>');
window.init();
